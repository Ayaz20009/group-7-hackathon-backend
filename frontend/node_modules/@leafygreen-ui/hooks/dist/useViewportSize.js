import { useEffect, useState } from 'react';
import debounce from 'lodash/debounce';
function getViewportSize() {
    return {
        width: window.innerWidth,
        height: window.innerHeight,
    };
}
export default function useViewportSize() {
    var _a = useState(null), viewportSize = _a[0], setViewportUpdateVal = _a[1];
    useEffect(function () {
        setViewportUpdateVal(getViewportSize());
        var calcResize = debounce(function () { return setViewportUpdateVal(getViewportSize()); }, 100);
        window.addEventListener('resize', calcResize);
        return function () { return window.removeEventListener('resize', calcResize); };
    }, []);
    return viewportSize;
}
//# sourceMappingURL=useViewportSize.js.map