export var Size = {
    Small: 'small',
    Default: 'default',
    Large: 'large',
    XLarge: 'xlarge',
};
export var sizeMap = {
    small: 14,
    default: 16,
    large: 20,
    xlarge: 24,
};
export function getGlyphTitle(name, title) {
    if (title === false) {
        // If title is null, we unset the title entirely, otherwise we generate one.
        return null;
    }
    if (title == null || title === true) {
        return name.replace(/([a-z])([A-Z])/g, '$1 $2') + " Icon";
    }
    return title;
}
//# sourceMappingURL=glyphCommon.js.map